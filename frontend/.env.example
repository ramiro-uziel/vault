# Frontend Environment Variables

# API URL - leave empty to use relative URLs (auto-detects based on hostname)
# For local development, this defaults to http://localhost:8080
# For production/tunnel access, leave empty to use same domain
VITE_API_URL=

# Optional: Custom app title
# VITE_APP_TITLE=Vault
